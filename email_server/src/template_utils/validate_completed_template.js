const validate_completed_template = (original_template, completed_template) => {
  //TODO validate a completed_template against the corresponding template
  return true;
};

export { validate_completed_template };