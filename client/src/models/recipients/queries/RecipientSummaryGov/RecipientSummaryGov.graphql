query RecipientSummaryGov($lang: String!) {
  root(lang: $lang) {
    gov {
      recipient_summary {
        id
        recipient_overview {
          year
          total_tf_exp
        }
        recipient_exp_summary {
          year
          recipient
          total_exp
          num_transfer_payments
        }
      }
    }
  }
}
