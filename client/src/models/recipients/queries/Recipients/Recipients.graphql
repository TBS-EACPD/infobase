query Recipients($lang: String!, $orgId: String) {
  root(lang: $lang) {
    org(org_id: $orgId) {
      recipients {
        year
        org_id
        program
        recipient
        city
        province
        country
        expenditure
      }
    }
  }
}
