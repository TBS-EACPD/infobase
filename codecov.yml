flags:
  client:
    paths:
      - ./client/
  e2e_shallow:
    paths:
      - ./client/
  server:
    paths:
      - ./server/
  form_backend:
    paths:
      - ./form_backend/

coverage:
  status:
    project:
      default:
        flags:
          - client
          - e2e_shallow
          - server
          - form_backend
      client:
        flags:
          - client
      e2e_shallow:
        flags:
          - e2e_shallow
        target: auto
      server:
        flags:
          - server
      form_backend:
        flags:
          - form_backend
        target: auto
