flags:
  client:
    paths:
      - ./client/
  basic_e2e:
    paths:
      - ./client/
  server:
    paths:
      - ./server/
  form_backend:
    paths:
      - ./form_backend/

coverage:
  status:
    project:
      default:
        flags:
          - client
          - basic_e2e
          - server
          - form_backend
      client:
        flags:
          - client
      basic_e2e:
        flags:
          - basic_e2e
        target: auto
      server:
        flags:
          - server
      form_backend:
        flags:
          - form_backend
        target: auto
