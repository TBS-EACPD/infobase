flags:
  client:
    paths:
      - ./client/
  server:
    paths:
      - ./server/
  form-backend:
    paths:
      - ./form_backend/

coverage:
  status:
    project:
      default:
        flags:
          - client
          - server
          - form-backend
      client:
        flags:
          - client
      server:
        flags:
          - server
      form-backend:
        flags:
          - form-backend
